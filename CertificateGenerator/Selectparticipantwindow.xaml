<Window x:Class="CertificateGenerator.SelectParticipantWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        Title="Výber účastníka" 
        Height="600" 
        Width="800"
        FontSize="14"
        WindowStartupLocation="CenterOwner"
        ResizeMode="CanResize"
        Background="{StaticResource BackgroundColor}">

    <Grid Margin="30">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
            <RowDefinition Height="Auto"/>
        </Grid.RowDefinitions>

        <!-- Header -->
        <Border Grid.Row="0" 
                Style="{StaticResource Card}" 
                Margin="0,0,0,20">
            <StackPanel>
                <TextBlock Text="👥" 
                           FontSize="38"
                           HorizontalAlignment="Center"
                           Margin="0,0,0,8"/>
                <TextBlock Text="Výber uloženého účastníka" 
                           FontSize="18"
                           FontWeight="Bold"
                           Foreground="{StaticResource TextPrimaryColor}"
                           HorizontalAlignment="Center"/>
            </StackPanel>
        </Border>

        <!-- Search Box -->
        <Border Grid.Row="1" 
                Style="{StaticResource Card}" 
                Margin="0,0,0,15">
            <StackPanel>
                <Label Content="🔍 Vyhľadávanie:" 
                       Style="{StaticResource ModernLabel}"
                       FontWeight="Bold"
                       Margin="0,0,0,8"/>
                <TextBox x:Name="TxtSearch" 
                         Style="{StaticResource ModernTextBox}"
                         TextChanged="TxtSearch_TextChanged"
                         ToolTip="Zadajte meno alebo registračné číslo"/>
            </StackPanel>
        </Border>

        <!-- Participants List -->
        <Border Grid.Row="2" 
                Style="{StaticResource Card}" 
                Margin="0,0,0,20">
            <Grid>
                <Grid.RowDefinitions>
                    <RowDefinition Height="Auto"/>
                    <RowDefinition Height="*"/>
                </Grid.RowDefinitions>

                <Label Grid.Row="0"
                       Content="Zoznam účastníkov"
                       Style="{StaticResource ModernLabel}"
                       FontSize="15"
                       FontWeight="Bold"
                       Margin="0,0,0,15"/>

                <Border Grid.Row="1"
                        BorderThickness="1"
                        BorderBrush="{StaticResource BorderColor}"
                        CornerRadius="6"
                        Background="{StaticResource SurfaceColor}">
                    <DataGrid x:Name="DgParticipants"
                              Style="{StaticResource ModernDataGrid}"
                              BorderThickness="0"
                              MouseDoubleClick="DgParticipants_MouseDoubleClick">
                        <DataGrid.Columns>
                            <DataGridTextColumn Header="Meno a priezvisko" 
                                                Binding="{Binding Name}" 
                                                Width="*"/>
                            <DataGridTextColumn Header="Dátum narodenia" 
                                                Binding="{Binding BirthDate, StringFormat=dd.MM.yyyy}" 
                                                Width="130"/>
                            <DataGridTextColumn Header="Reg. číslo" 
                                                Binding="{Binding RegistrationNumber}" 
                                                Width="120"/>
                            <DataGridTextColumn Header="Použité" 
                                                Binding="{Binding UsageCount}" 
                                                Width="80"/>
                        </DataGrid.Columns>
                    </DataGrid>
                </Border>
            </Grid>
        </Border>

        <!-- Buttons -->
        <Grid Grid.Row="3">
            <Grid.ColumnDefinitions>
                <ColumnDefinition Width="Auto"/>
                <ColumnDefinition Width="*"/>
                <ColumnDefinition Width="Auto"/>
            </Grid.ColumnDefinitions>

            <Button Grid.Column="0"
                    Content="🗑️ Odstrániť" 
                    Style="{StaticResource DangerButton}"
                    Width="130"
                    Height="44"
                    FontSize="14"
                    Click="Delete_Click"/>

            <StackPanel Grid.Column="2"
                        Orientation="Horizontal">
                <Button Content="✓ Vybrať" 
                        Style="{StaticResource SuccessButton}"
                        Width="140"
                        Height="44"
                        FontSize="14"
                        FontWeight="Bold"
                        Margin="0,0,15,0"
                        IsDefault="True"
                        Click="Select_Click"/>
                <Button Content="✖️ Zrušiť" 
                        Style="{StaticResource SecondaryButton}"
                        Width="120"
                        Height="44"
                        FontSize="14"
                        IsCancel="True"
                        Click="Cancel_Click"/>
            </StackPanel>
        </Grid>
    </Grid>
</Window>
