<Window x:Class="CertificateGenerator.ManageOrganizerWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        Title="Správa organizátorov" 
        Height="750" 
        MinHeight="380" 
        Width="800"
        FontSize="14"
        WindowStartupLocation="CenterScreen"
        WindowState="Maximized"
        ResizeMode="CanResize"
        Background="{StaticResource BackgroundColor}">

    <Grid Margin="10,10,0,10">
        <Grid.ColumnDefinitions>
            <ColumnDefinition Width="*"/>
            <ColumnDefinition Width="15"/>
            <ColumnDefinition Width="350"/>
        </Grid.ColumnDefinitions>

        <!-- LEFT SIDE: List of organizers -->
        <Grid Grid.Column="0">
            <Grid.RowDefinitions>
                <RowDefinition Height="Auto"/>
                <RowDefinition Height="Auto"/>
                <RowDefinition Height="*"/>
                <RowDefinition Height="Auto"/>
            </Grid.RowDefinitions>

            <!-- Organizers List -->
            <Border Grid.Row="2" 
                    Style="{StaticResource Card}" 
                    Margin="0,0,0,20">
                <Grid>
                    <Grid.RowDefinitions>
                        <RowDefinition Height="Auto"/>
                        <RowDefinition Height="Auto"/>
                        <RowDefinition Height="*"/>
                        <RowDefinition Height="Auto"/>
                    </Grid.RowDefinitions>

                    <StackPanel Grid.Row="0" Margin="0,0,0,15">
                        <TextBlock Text="Uložení organizátori" 
                               FontSize="16"
                               FontWeight="Bold"
                               Foreground="{StaticResource TextPrimaryColor}"
                                   Margin="0,0,0,15"
                               HorizontalAlignment="Center"/>
                        <Label Content="🔍 Vyhľadávanie:" 
                               Style="{StaticResource ModernLabel}"
                               FontWeight="Bold"
                               Margin="0,0,0,8"/>
                        <TextBox x:Name="TxtSearch" 
                                 Style="{StaticResource ModernTextBox}"
                                 TextChanged="TxtSearch_TextChanged"
                                 ToolTip="Zadajte názov organizátora"/>
                    </StackPanel>

                    <Label Grid.Row="1" 
                           Content="Zoznam organizátorov"
                           Style="{StaticResource ModernLabel}"
                           FontSize="15"
                           FontWeight="Bold"
                           Margin="0,0,0,15"/>

                    <Border Grid.Row="2"
                            BorderThickness="1"
                            BorderBrush="{StaticResource BorderColor}"
                            CornerRadius="6"
                            Background="{StaticResource SurfaceColor}">
                        <DataGrid x:Name="DgOrganizers"
                                  Style="{StaticResource ModernDataGrid}"
                                  BorderThickness="0"
                                  SelectionChanged="DgOrganizers_SelectionChanged"
                                  MouseDoubleClick="DgOrganizers_MouseDoubleClick">
                            <DataGrid.Columns>
                                <DataGridTextColumn Header="Názov" 
                                                    Binding="{Binding Name}" 
                                                    Width="*"/>
                                <DataGridTextColumn Header="Popis" 
                                                    Binding="{Binding Description}" 
                                                    Width="2*"/>
                                <DataGridTextColumn Header="Použité" 
                                                    Binding="{Binding UsageCount}" 
                                                    Width="80"/>
                            </DataGrid.Columns>
                        </DataGrid>
                    </Border>
                </Grid>
            </Border>

            <!-- Bottom Buttons -->
            <Grid Grid.Row="3">
                <Grid.ColumnDefinitions>
                    <ColumnDefinition Width="Auto"/>
                    <ColumnDefinition Width="*"/>
                    <ColumnDefinition Width="Auto"/>
                </Grid.ColumnDefinitions>

                <Button Grid.Column="0"
                        Content="🗑️ Odstrániť" 
                        Style="{StaticResource DangerButton}"
                        FontSize="14"
                        Click="Delete_Click"/>

                <StackPanel Grid.Column="2"
                            Orientation="Horizontal">
                    <Button Content="✓ Vybrať" 
                            x:Name="BtnSelect"
                            Style="{StaticResource SuccessButton}"
                            FontSize="14"
                            FontWeight="Bold"
                            Margin="10,0,10,0"
                            IsDefault="True"
                            Click="Select_Click"/>
                    <Button Content="✖️ Zrušiť" 
                            Style="{StaticResource SecondaryButton}"
                            FontSize="14"
                            IsCancel="True"
                            Click="Cancel_Click"/>
                </StackPanel>
            </Grid>
        </Grid>

        <!-- RIGHT SIDE: Add/Edit organizer form -->
        <Border Grid.Column="2"
                Style="{StaticResource Card}">
            <Grid>
                <Grid.RowDefinitions>
                    <RowDefinition Height="Auto"/>
                    <RowDefinition Height="*"/>
                    <RowDefinition Height="Auto"/>
                </Grid.RowDefinitions>

                <!-- Form Header -->
                <TextBlock Grid.Row="0"
                           x:Name="TxtFormTitle"
                           Text="Nový organizátor" 
                           FontSize="16"
                           FontWeight="Bold"
                           Foreground="{StaticResource TextPrimaryColor}"
                           HorizontalAlignment="Center"
                           Margin="0,0,0,20"/>

                <!-- Form Fields -->
                <StackPanel Grid.Row="1">
                    <Label Content="Názov organizátora *"
                           Style="{StaticResource ModernLabel}"
                           FontWeight="Bold"
                           Margin="0,0,0,8"/>
                    <TextBox x:Name="TxtOrganizerName"
                             Style="{StaticResource ModernTextBox}"
                             Margin="0,0,0,20"
                             ToolTip="Povinné pole"/>

                    <Label Content="Popis"
                           Style="{StaticResource ModernLabel}"
                           FontWeight="Bold"
                           Margin="0,0,0,8"/>
                    <TextBox x:Name="TxtDescription"
                             Style="{StaticResource ModernTextBox}"
                             Height="150"
                             TextWrapping="Wrap"
                             AcceptsReturn="True"
                             VerticalScrollBarVisibility="Auto"
                             ToolTip="Nepovinné"/>
                </StackPanel>

                <!-- Form Buttons -->
                <StackPanel Grid.Row="2"
                            Margin="0,20,0,0">
                    <Button Content="💾 Uložiť organizátora"
                            x:Name="BtnSave"
                            Style="{StaticResource ModernButton}"
                            FontSize="14"
                            FontWeight="Bold"
                            Margin="0,0,0,10"
                            Click="SaveOrganizer_Click"/>

                    <Button Content="🔄 Vyčistiť formulár"
                            x:Name="BtnClear"
                            Style="{StaticResource SecondaryButton}"
                            FontSize="13"
                            Click="ClearForm_Click"/>

                    <TextBlock x:Name="TxtEditMode"
                               Text="Režim úpravy"
                               Foreground="{StaticResource PrimaryColor}"
                               FontSize="12"
                               FontStyle="Italic"
                               HorizontalAlignment="Center"
                               Margin="0,10,0,0"
                               Visibility="Collapsed"/>
                </StackPanel>
            </Grid>
        </Border>
    </Grid>
</Window>
